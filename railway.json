{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun run db:generate && next build && cp -r .next/static .next/standalone/.next/ && cp -r public .next/standalone/",
    "watchPatterns": ["src/**", "prisma/**", "public/**", "package.json", "next.config.ts"]
  },
  "deploy": {
    "startCommand": "mkdir -p /data && npx prisma db push --accept-data-loss --skip-generate && bun run prisma/seed.ts && cd .next/standalone && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
